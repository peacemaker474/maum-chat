import styled from '@emotion/styled';

const MainWrapper = styled.main`
  width: 100%;
  height: 100%;
  background-color: blue;
`;

function Main () {
  return (
    <MainWrapper>

    </MainWrapper>
  );
}

export default Main;